{"method":"run","code":"net.minecraft.client.Minecraft mc = net.minecraft.client.Minecraft.getInstance(); if (mc.player == null || mc.level == null) { System.out.println(\"нет игрока/мира\"); return; } java.util.concurrent.CountDownLatch latch = new java.util.concurrent.CountDownLatch(1); mc.execute(() -> { try { net.minecraft.world.level.Level level = mc.level; net.minecraft.core.BlockPos pos = mc.player.blockPosition(); int cx = Math.floorDiv(pos.getX(), 16); int cz = Math.floorDiv(pos.getZ(), 16); int count = 0; for (int x = 0; x < 16; x++) { for (int z = 0; z < 16; z++) { for (int y = level.getMinY(); y < level.getMaxY(); y++) { level.setBlock(new net.minecraft.core.BlockPos((cx * 16) + x, y, (cz * 16) + z), net.minecraft.world.level.block.Blocks.AIR.defaultBlockState(), 3, 0); count++; } } } System.out.println(\"Чанк стёрт: \" + count + \" блоков воздуха\"); } finally { latch.countDown(); } }); try { latch.await(60, java.util.concurrent.TimeUnit.SECONDS); } catch (java.lang.InterruptedException e) { Thread.currentThread().interrupt(); }"}
